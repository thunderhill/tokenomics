server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: client-side routing, if any is added later, still resolves.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
